import getTransactionsByUsers from './getTransactionsByUsers';
import makeTransactionRequest from './makeTransactionRequest';

const main = {
  getTransactionsByUsers,
  makeTransactionRequest,
};

export { main, getTransactionsByUsers, makeTransactionRequest };
export default main;

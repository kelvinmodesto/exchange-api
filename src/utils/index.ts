import getRates from './getRates';
import { makeTransaction } from './makeTransation';

const main = {
  getRates,
  makeTransaction,
};

export { main, getRates, makeTransaction };
export default main;

import { ContextStrategy } from './base';
import { SQLiteStrategy } from './sqlite';

const main = {
  ContextStrategy,
  SQLiteStrategy,
};

export { main, ContextStrategy, SQLiteStrategy };
export default main;
